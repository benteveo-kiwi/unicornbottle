# UnicornBottle common.

This repository contains Python code that is relevant to both the
`unicornbottle-cli` and the `unicornbottle-httpproxy` projects. In order to
avoid repetition, relevant code is stored here and imported from both these
repositories using `git submodule`.
